export enum StatusEnum {
  initial = "initial",
  running = "running",
  success = "success",
  error = "error",
}

export enum VariantEnums {
  info = "info",
  error = "error",
  success = "success",
  warning = "warning",
  default = "default",
}
